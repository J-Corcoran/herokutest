App successfully deployed to Heroku :-)

To update the app. (as heroku app already added as a remote using CLI command: heroku git:remote -a simpleserver07
You can now simply: 
git add .
git commit -m "My commit"
git push heroku main


...Can also git push to update to git repo. 